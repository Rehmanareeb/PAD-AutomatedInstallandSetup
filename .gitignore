# Ignore all files
*

# Don't ignore directories so we can re-include files within them
!*/

# Track only these
!/.gitignore
!/README.md
!/Setup_PAD_Final.ps1

# Never track agent/tooling state, whatever the rules above allow
.claude/
